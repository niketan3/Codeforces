#include<bits/stdc++.h>
using namespace std;
int main (){
    int t;
    cin>>t;
    while(t--)
    {
        int len,x;
        cin>>len>>x;

        for (int i=1;i<=len;i++)
        {
            for (int j=1;j<=len;j++)
            {
                if(j-i+1).
            }
        }
    }
    return 0;
}